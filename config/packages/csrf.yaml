# Use session-backed CSRF for login (stateless CSRF breaks behind some proxies)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
